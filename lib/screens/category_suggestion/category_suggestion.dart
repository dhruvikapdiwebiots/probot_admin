import 'package:flutter/cupertino.dart';

class CategorySuggestion extends StatelessWidget {
  const CategorySuggestion({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Column();
  }
}
